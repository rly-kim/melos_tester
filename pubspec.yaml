name: melos_tester_workspace
description: A starting point for Dart libraries or applications.
repository: https://github.com/rly-kim/melos_tester

environment:
  sdk: ^3.2.5

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
  melos: ^3.0.1
